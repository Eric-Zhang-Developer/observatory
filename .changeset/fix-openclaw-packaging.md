---
"@contextcompany/openclaw": patch
---

Fix openclaw plugin packaging metadata: rename `openclaw.extension` to `openclaw.extensions` (plural array) and add `openclaw.plugin` pointer to manifest file
